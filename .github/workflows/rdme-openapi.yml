# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2023-03-24T18:36:48.808Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: ReadMe GitHub Action 🦉

on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `main`
      # Check out GitHub's docs for more info on configuring this:
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - main
      - 'releases/**'
      - 'release/**'

jobs:
  rdme-openapi:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v6

      - name: Run `openapi` command 🚀
        uses: readmeio/rdme@v10
        with:
          rdme: openapi _build/api.yaml --key=${{ secrets.README_API_KEY }} --id=${{ secrets.API_README_VERSION_2_2_0_ID }}
