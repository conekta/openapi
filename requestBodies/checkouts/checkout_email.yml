description: requested field for sms checkout        
content:
  application/json:
    schema:  
      type: object
      required:
        - email
      properties:
        email: 
          type: string
          example: "example@conekta.com"
          format: email
required: true
