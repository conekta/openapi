post:
  tags:
    - orders
  operationId: orderRefund
  summary: A refunded order describes the items, amount, and reason an order is being refunded.
  responses:
    200:
      description: successful
      content:
        application/vnd.conekta-v2.0.0+json:
          schema:
            $ref: ../../schemas/orders/order_response.yml
    401:
     $ref: '../errors/401.yml'
    404:
     $ref: '../errors/404.yml'
    422:
     $ref: '../errors/422.yml'
    500:
     $ref: '../errors/500.yml'
  security:
    - bearerAuth: []
  requestBody:
   $ref: '../../requestBodies/orders/order_refunds_request.yml'
  parameters:
    - $ref: '../../parameters/commons/path_param/id.yml'
    - $ref: '../../parameters/commons/headers/accept_language.yml'
